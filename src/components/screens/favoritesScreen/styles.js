import { StyleSheet } from "react-native";

export const favoriteScreenStyles = StyleSheet.create({
    title: {
        marginBottom: 30,
        paddingBottom: 10,
        fontSize: 35,
        fontWeight: 'bold',
        marginHorizontal: 20
    },
})